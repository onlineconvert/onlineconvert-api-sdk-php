language: php

cache:
  directories:
    - $HOME/.composer/cache

matrix:
  include:
    - php: 5.6
    - php: 7.0
    - php: 7.1

before_install:
  - composer selfupdate

install:
  - composer install

script: bin/phpunit -c phpunit.xml